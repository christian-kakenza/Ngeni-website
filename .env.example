# ============================================================
# NGENI — Variables d'Environnement — TEMPLATE PRODUCTION
# Copiez ce fichier en .env et remplissez chaque valeur.
# NE JAMAIS committer .env sur Git !
# ============================================================

# ----- Base de Données (PostgreSQL via Supabase) -----------
# Récupérer depuis : Supabase Dashboard → bouton "Connect" → Connection pooling
# DATABASE_URL  = Transaction mode (port 6543) — utilisé par l'app
# DIRECT_URL    = Session mode (port 5432)    — utilisé par Prisma migrate/push
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-1-eu-west-1.pooler.supabase.com:5432/postgres"

# ----- Auth.js (NextAuth v5) --------------------------------
# Générer avec : openssl rand -base64 32
AUTH_SECRET="VOTRE_SECRET_32_CHARS_MIN_ICI"
AUTH_URL="https://ngeni.ai"
NEXTAUTH_URL="https://ngeni.ai"

# ----- Email Notifications (Resend) -------------------------
# Compte : resend.com — Plan gratuit : 100 emails/jour
# Clé à récupérer depuis : resend.com/api-keys
RESEND_API_KEY="re_..."
# Email de destination pour les notifications de contact
EMAIL_TO="christian.kakenza0@gmail.com"

# ----- OpenAI (Fonctionnalités IA) --------------------------
# Optionnel — laisser vide si non utilisé
OPENAI_API_KEY="sk-..."

# ----- Environnement ----------------------------------------
NODE_ENV="production"
