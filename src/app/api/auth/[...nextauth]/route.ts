import { handlers } from "@/lib/auth";

// ============================================================
// Auth.js v5 — Route Handler Next.js App Router
// Endpoints : GET /api/auth/* + POST /api/auth/*
// ============================================================

export const { GET, POST } = handlers;
